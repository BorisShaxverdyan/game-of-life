# Game of life

## Start development

Run this command in one bash tab:

```
$ npm run watch
```

And run this command in another bash tab:

```
$ npm run server
```

Then go to http://localhost:3000/ page
